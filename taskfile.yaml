version: '3'

tasks:
  default:
    desc: list tasks
    cmds:
      - task --list-all --sort none

  test:
    desc: run tests
    cmds:
      - uv run pytest -v

  format:
    desc: format
    cmds:
      - uv run ruff format
