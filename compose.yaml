services:
  redmine-for-test:
    image: redmine:6.1
    ports:
      - 3000:3000

  redmine-for-dogfooding:
    image: redmine:6.1
    profiles:
      - dogfooding
    restart: always
    ports:
      - 3001:3000
    volumes:
      - redmine-dogfooding-files:/usr/src/redmine/files
      - redmine-dogfooding-sqlite:/usr/src/redmine/sqlite

  redmine-for-demo:
    profiles:
      - demo
    image: redmine:6.1
    ports:
      - 3002:3000

volumes:
  redmine-dogfooding-files:
  redmine-dogfooding-sqlite:
