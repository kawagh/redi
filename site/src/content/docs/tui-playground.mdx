---
title: TUI playground
description: Operate a reproduction of the redi TUI in your browser.
tableOfContents: false
---

import TryTui from "../../components/TryTui.astro";

A reproduction of the TUI that `redi --tui` opens.

<TryTui lang="en" />

## What to try

| Key | |
| --- | --- |
| `j` / `k` | Move through the list. The right pane follows |
| `Tab` | Switch between Issues, Time entries and Wiki |
| `/` | Search. Type, then `Enter`. `Esc` clears it |
| `f` | Open the filter. `Tab` / `h` `l` switch column, `j` `k` move, `Enter` applies, `c` clears everything |
| `?` | Help for the current tab |
| `Enter` | On the Wiki tab, load the page body |
| `q` | Quit |
